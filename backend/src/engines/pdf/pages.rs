//! Page-management implementation moved to `pages_disk`.
//!
//! This module is intentionally kept as an empty compatibility module for one
//! release cycle so downstream paths that still reference the module continue
//! to resolve without retaining the obsolete in-memory implementation.
