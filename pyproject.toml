[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pdfin-cli"
version = "0.1.0"
description = "CLI tool for PDF processing (optional extension)"
requires-python = ">=3.9"
